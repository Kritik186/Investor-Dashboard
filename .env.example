# Backend
# SQLite (default, file created in backend folder):
# DATABASE_URL=sqlite:///./insider.db
# PostgreSQL:
DATABASE_URL=postgresql://postgres:kritik@localhost:5432/InsiderDB
SEC_USER_AGENT=InsiderDashboard/1.0 (kritik.ajmani@bain.com)
# Set to 0 or false if you get SSL certificate errors (e.g. corporate proxy). Less secure; use only in trusted envs.
# SEC_VERIFY_SSL=0

# Frontend (optional; defaults used in dev)
NEXT_PUBLIC_API_URL=http://localhost:8000
